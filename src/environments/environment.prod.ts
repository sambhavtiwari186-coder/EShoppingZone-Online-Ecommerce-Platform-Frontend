export const environment = {
  production: true,
  // We recommend using a relative URL or your production backend domain here:
  apiUrl: 'https://eshoppingzone-backend.onrender.com' // Replace with your production remote backend API URL when available
};
